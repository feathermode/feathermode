/* Check the type of a variable by passing it into this function. It will return a boolean.
	@params {string} location - location to be cleaned before reinsertion
*/
export function checkType(variable, type) {
	// String
		type === 'string' || 's' ? typeof variable === 'string' : ;s
	
	// Array

	
	// Object

	
	// Boolean

	
	// 
}